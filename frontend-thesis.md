# 前端毕业设计文档（论文结构版）

## 摘要
本文围绕“项目管理平台”的前端系统设计与实现展开，基于 React 18 + TypeScript 构建，采用 Vite 作为构建工具，使用 React Router（Data Mode）完成路由组织，Zustand 完成全局状态管理，并结合 Ant Design 与 Tailwind CSS 实现统一、可复用的界面风格。系统前端提供登录/注册、工作台、空间与工作项、任务详情、收藏、通知与空间设置等功能模块，通过统一请求层实现鉴权与错误处理，并通过流程图可视化与富文本组件提升可视化表达能力。本文从需求分析、系统设计、模块实现、测试与总结等方面系统描述前端实现过程，为同类毕业设计项目提供参考。

**关键词**：React；Vite；Zustand；项目管理；流程图；前端架构

---

## 第一章 绪论

### 1.1 研究背景
团队协作场景下的项目管理系统需要具备任务可视化、流程追踪、权限管理与协作通知等能力。前端作为用户交互入口，要求具备清晰的信息架构、稳定的状态管理与统一的交互体验。基于现代前端技术栈构建的系统能够更好地支持复杂业务。

### 1.2 研究目的与意义
通过搭建一套完整的项目管理平台前端系统，验证 React + TypeScript 在复杂业务系统中的可行性，并形成一套可复用的工程化与模块化实现范式。

### 1.3 研究内容
本文重点围绕以下内容展开：  
- 前端整体架构与模块划分  
- 路由与布局组织  
- 关键模块设计与实现  
- 统一请求封装与鉴权策略  
- 交互与性能优化  
- 测试与验收方案  

---

## 第二章 相关技术概述

### 2.1 React 18 + TypeScript
React 组件化思想提升 UI 复用性；TypeScript 增强类型安全与代码可维护性。

### 2.2 Vite
基于原生 ESM 的构建工具，支持快速冷启动与热更新，适合中大型前端项目。

### 2.3 React Router（Data Mode）
用于组织多层级路由与嵌套布局，并为业务模块提供清晰的导航关系。

### 2.4 Zustand
轻量化状态管理库，适合全局用户态与通知态等核心状态场景。

### 2.5 Ant Design 与 Tailwind CSS
Ant Design 提供完整的组件能力，Tailwind 提供高效的样式组合方式。

### 2.6 Cytoscape.js 与 Tiptap
Cytoscape.js 负责流程图渲染与交互，Tiptap 负责富文本编辑。

---

## 第三章 需求分析

### 3.1 功能需求
- 用户登录与注册  
- 工作台任务统计与分类列表  
- 空间管理与空间主页  
- 工作项类型管理  
- 任务列表与任务详情  
- 收藏管理  
- 通知中心  
- 空间设置（基础信息、权限管理）

### 3.2 非功能需求
- 统一错误提示与异常兜底  
- 页面响应与交互一致性  
- 基础权限控制与会话安全  
- 可维护与可扩展的模块化结构

---

## 第四章 系统总体设计

### 4.1 分层结构
前端采用“页面模块层 + 组件层 + 基础设施层”的分层结构：  
- 基础设施层：请求封装、鉴权、错误处理  
- 组件层：复用组件与复杂交互组件  
- 页面层：业务域页面与路由组织

### 4.2 路由与布局设计
系统以 `MainLayout` 为主布局，空间域使用 `SpaceWorkItemLayout` 实现空间导航与主内容区的分离。  

路由结构概览：  
- `/` 工作台  
- `/space/:spaceId/overview` 空间主页  
- `/space/:spaceId/:workItemId` 工作项列表  
- `/space/:spaceId/:workItemId/:taskId/detail` 任务详情  
- `/space/:spaceId/settings/*` 空间设置  
- `/favorites` 收藏  
- `/notifications` 通知  
- `/login` 登录  

---

## 第五章 模块设计与实现

### 5.1 登录与注册模块
登录/注册采用表单校验后调用接口完成鉴权，成功后写入 `localStorage.userInfo`，并同步 Zustand 用户状态，完成会话建立。

### 5.2 工作台模块
工作台展示任务统计与分类列表，通过 `/tasks/dashboard` 与 `/tasks/dashboard/stats` 获取数据。分类切换后请求对应类型数据，并支持行点击跳转任务详情。

### 5.3 空间与工作项模块
空间模块负责加载用户空间与工作项列表。进入空间时自动导航到第一个空间主页，工作项列表加载失败时提供重试入口。

### 5.4 空间主页模块
空间主页通过富文本组件展示内容，管理员可进入编辑模式并保存。保存后更新空间主页内容并反馈提示。

### 5.5 工作项列表模块
工作项列表支持分页、内联编辑与新建任务。编辑字段通过 `PUT /tasks/:id` 保存，创建任务时生成 `fieldStatusList` 并调用 `POST /tasks`。

### 5.6 任务详情模块
任务详情由流程图与底部详情区构成，流程图基于 Cytoscape 渲染并支持节点点击与状态映射。进入详情页后写入最近浏览记录。

### 5.7 收藏模块
收藏模块聚合用户收藏任务，侧栏点击任务后复用任务详情组件进行展示。

### 5.8 通知模块
通知模块支持分页与滚动加载，提供“全部/未读/@我”筛选，并支持单条或全部标记已读。

### 5.9 空间设置模块
空间设置包含空间信息、工作项管理、权限管理三部分：  
- 空间信息：名称与图标更新、删除空间  
- 工作项管理：创建、编辑、删除工作项类型，进入字段/角色/流程配置  
- 权限管理：分页成员列表、搜索添加成员、权限分组展示  

---

## 第六章 核心实现说明

### 6.1 统一请求封装
请求层支持超时控制、统一响应解析、自动注入 Token，并对 401 做会话清理与跳转处理。

### 6.2 状态管理
Zustand 用于管理用户信息与通知未读数，避免多层组件传参。

### 6.3 流程图可视化
`ProcessView` 将业务节点映射为 Cytoscape 元素，通过控制点计算曲线连线，并根据节点状态显示不同颜色。

### 6.4 富文本编辑
空间主页使用 Tiptap 富文本组件，支持富文本内容编辑与保存。

---

## 第七章 测试与结果

### 7.1 功能测试
- 登录/注册校验与登录态保持  
- 空间切换与工作项加载  
- 任务创建、字段编辑与详情展示  
- 收藏列表与详情显示  
- 通知加载与已读状态更新  

### 7.2 兼容性测试
在 Chrome 与 Edge 浏览器上进行基础兼容性验证，常用分辨率下布局展示正常。

---

## 第八章 总结与展望

### 8.1 总结
系统前端实现了完整的项目管理核心功能，具备清晰的架构层次与稳定的业务交互流程，能够满足毕业设计要求。

### 8.2 展望
- 进一步完善权限体系与角色细粒度控制  
- 引入自动化测试与持续集成  
- 支持更多数据可视化与分析能力  

---

## 附录 A 目录结构说明

```
src/
├─ api/                 # 通用请求封装
├─ assets/              # 静态资源
├─ components/          # 通用组件（流程图、任务详情等）
├─ constants/           # 常量与枚举
├─ hooks/               # 通用 Hook
├─ layouts/             # 全局布局
├─ pages/               # 页面模块
├─ router/              # 路由配置
├─ store/               # 全局状态
├─ styles/              # 全局样式
├─ types/               # 类型声明
└─ utils/               # 工具方法
```

---

## 附录 B 核心接口说明（前端视角）

| 模块 | 接口 | 说明 |
| --- | --- | --- |
| 登录 | `/auth/login` | 用户登录 |
| 注册 | `/auth/register` | 用户注册 |
| 工作台 | `/tasks/dashboard` | 分类任务列表 |
| 工作台 | `/tasks/dashboard/stats` | 统计数据 |
| 空间 | `/spaces` | 空间列表 |
| 空间 | `/spaces/:id` | 空间详情 |
| 工作项 | `/workItems/:id` | 工作项详情 |
| 工作项 | `/workItems/:id/fields` | 字段配置 |
| 任务 | `/tasks` | 新建任务 |
| 任务 | `/tasks/:id` | 编辑任务 |
| 流程 | `/workflow-types/workItem/:id` | 流程模板 |
| 通知 | `/notices` | 通知列表 |
| 通知 | `/notices/unread-count` | 未读数 |

---

## 附录 C 交互与可用性说明

### C.1 通用交互
- 使用 `Spin` 与 `Empty` 统一加载态与空态  
- 表单统一规则校验与错误提示  
- 顶部栏布局保持一致的“图标 + 标题 + 操作”模式  

### C.2 错误与异常处理
- 请求层统一处理超时与网络错误  
- 401 自动清理会话并跳转登录  
- 提示统一使用 Ant Design Notification  

---

## 附录 D 测试用例示例

### D.1 登录功能
- 输入合法账号密码，返回 `userInfo` 并跳转首页  
- 输入错误密码，提示“账号/密码错误”  

### D.2 任务列表
- 切换不同分类标签，列表刷新正确  
- 编辑字段后保存成功且数据刷新  

### D.3 通知功能
- 滚动到底自动加载下一页  
- 点击“全部标为已读”后未读数清零  
